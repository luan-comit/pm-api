extends ./layout.pug
block contents
    //Jumbotron
    .container
        .jumbotron
            form.form-signin(action='/register', method='POST') 
                h3.form-signin-heading Register new user
                label.sr-only(for='inputEmail') Email address
                input#inputEmail.form-control(type='email', name='email', placeholder='Your email address: user@abc.com', required='', autofocus='', pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$")
                label.sr-only(for='inputPassword') Password
                input#inputPassword.form-control(type='password', name='password', placeholder='Your password: at least 6, characters and/or digits only', required='', pattern="[a-z0-9]{6,}$")
                br
                button.btn.btn-lg.btn-primary.btn-block(type='submit') Register

            //Site footer
        footer.footer
            p © Price Monitor - NodeJS Project last updated Nov. 2023